export interface RecordingMetadata {
    id: string;
    MCAddress: string;
    name: string;
    date: Date;
    recordingLength: number;
    filePath: string;
    fileSize: string;
    recordingCount: number;
}
